/**
 * Deprecated local backdrop. The global theme (html[data-pc-bg]) now paints
 * every page, so this renders nothing to avoid covering the selected theme.
 */
export function ResourcesFX() {
  return null;
}
